* Move CID out of mode-control-register and into a proper feature or extension.
  (It adds an instruction, `INVLCID`, so I suppose it should be an extension.)
  Mention it in mode-control-register.md and each extension needs to clearly
  indicate where CIDs go in `VM_ROOT` and soft TLB entries.
* Mention of "global" pages, an optional feature, which allows some marked pages
  to dodge getting shot down.
  The `G` bit should be clearly marked in extensions supporting it,
  but treated as reserved when the feature is not available.
  Allocate a feature bit for global pages.
* Define `#PF` exceptions. `INT_DATA` will be used to carry the error code
  (not present, bad W permission, bad U permission, etc.... + soft TLB miss)

* Write 16-bit paging extension; address breakdown and PTE format are decided already.
  `VM_ROOT` carries the most signifcant 14 address bits for the table.
  (Note that 14 is more than the 13 bits in each entry, because two tables per frame
  are supported).
  Still needs:
  * TLB entry format
  * ?
* Write 32-bit paging extension; address breakdown and PTE format
  are decided already.
  Needs:
  * `VM_ROOT` format
  * TLB entry format
  * ?